name: Automated Data Preprocessing

on:
  push:
    paths:
      - 'preprocessing/automate_Mohammad-Aris-Darmawan.py'
      - '.github/workflows/automate_preprocessing.yml' 
  workflow_dispatch:

permissions:
  contents: write

jobs:
  preprocess:
    runs-on: ubuntu-latest

    env:
      KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
      KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3
      with:
        ref: main         
        fetch-depth: 0    

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy scikit-learn kagglehub

    - name: Run Preprocessing Script
      run: |
        python preprocessing/automate_Mohammad-Aris-Darmawan.py

    - name: Commit and Push Preprocessed Data
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Tarik data terbaru dari server SEBELUM melakukan penambahan file
        git pull origin main --rebase
        
        # Tambahkan file preprocessed
        git add preprocessing/hotel_bookings_preprocessed.csv
        
        # Lakukan commit
        git commit -m "Auto-update: Generate dataset preprocessed terbaru" || echo "Tidak ada perubahan data"
        
        # Paksa dorong ke branch main
        git push origin main